import 'package:flutter/material.dart';

/// Paleta de colores optimizada para apps de deportes y TV en vivo.
class AppColors {
  // Colores principales de marca
  static const Color primary = Color(0xFFFF6F00); // Naranja vibrante (F1)
  static const Color secondary = Color(0xFF1976D2); // Azul deportivo (fútbol/TV)
  static const Color accent = Color(0xFF00E676); // Verde acento (eventos en vivo)

  // Colores de estado
  static const Color success = Color(0xFF43A047); // Verde éxito
  static const Color error = Color(0xFFD32F2F); // Rojo alerta
  static const Color warning = Color(0xFFFFEB3B); // Amarillo advertencia
  static const Color info = Color(0xFF29B6F6); // Azul informativo

  // Colores de fondo y superficies
  static const Color background = Color(0xFF121212); // Fondo general oscuro
  static const Color surface = Color(0xFF1E1E1E); // Fondo de tarjetas
  static const Color modalBackground = Color(0xFFF5F5F5); // Fondo de modales claro

  // Colores de texto
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Color(0xFFB0BEC5); // Gris claro

  // Colores de bordes y detalles
  static const Color border = Color(0xFF424242);
  static const Color borderStrong = Color(0xFF757575);

  // Colores utilitarios
  static const Color hourBackground = Color(0xFF263238); // Fondo para hora
  static const Color gray = Color(0xFF37474F); // Gris utilitario
}